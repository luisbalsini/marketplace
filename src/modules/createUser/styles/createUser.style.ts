import styled from 'styled-components/native';
import { theme } from '../../../shared/themes/theme';

export const ContainerCreateUser = styled.ScrollView`
  background-color: ${theme.colors.neutralTheme.write};
  padding: 16px;
`;
