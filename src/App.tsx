import { SafeAreaView } from 'react-native';
import React from 'react';
import Login from './modules/login';

const App = () => {
  return (
    <SafeAreaView>
      <Login />
    </SafeAreaView>
  );
};

export default App;
